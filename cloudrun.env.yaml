# Example Cloud Run environment variables
# You can use this file to manage environment variables for Cloud Run
# Reference in cloudbuild.yaml with --env-vars-file=cloudrun.env.yaml
DATABASE_URL: file:/var/www/db/jobs.db?cache=shared&mode=rwc&_journal_mode=WAL
PORT: 8080
